# submissions

nospare-datasci 学習プラットフォームの提出物を管理するリポジトリです。
学習者が `nd.submit()` を呼ぶと、このリポジトリに Issue が自動作成されます。

## Issue ラベル

| ラベル | 意味 |
|--------|------|
| `submission` | 学習者からの提出（自動付与） |
| `needs-review` | レビュー待ち（自動付与） |
| `reviewed` | レビュー済み（手動で付与） |
